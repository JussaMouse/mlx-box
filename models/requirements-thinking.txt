# Thinking backend venv deps (isolated to avoid mlx-openai-server conflicts)
# Install with:
#   /Users/env/server/mlx-box/models/venvs/thinking/bin/python -m pip install -r models/requirements-thinking.txt
requests>=2.32.3
tomlkit>=0.12.0
mlx==0.31.0
git+https://github.com/ml-explore/mlx-lm.git
mlx-vlm[torch]>=0.3.12
